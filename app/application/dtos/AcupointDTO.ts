export interface CreateAcupointDTO {
  acupointCode: string;
  acupointName: string;
  isBilateral: boolean;
}

export interface UpdateAcupointDTO {
  acupointCode: string;
  acupointName: string;
  isBilateral: boolean;
}